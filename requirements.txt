# Dependencies for scripts/stl-to-scad.py
# (analyze-stl.py has zero dependencies — stdlib only)
trimesh>=4.0
numpy>=1.24
shapely>=2.0        # needed for sliced mode
rtree>=1.0          # needed for sliced mode
fast-simplification>=0.1  # needed for --reduce option
